apiVersion: v1
kind: Service
metadata:
  name: devops-lab-service
  namespace: devops-lab
  labels:
    app: devops-lab
spec:
  type: LoadBalancer
  selector:
    app: devops-lab
  ports:
  - name: http
    port: 80
    targetPort: 3000
    protocol: TCP
  sessionAffinity: None
